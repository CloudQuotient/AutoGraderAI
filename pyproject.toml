[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra"

[tool.ruff]
select = ["E", "F", "W"]

[project]
name = "autograderai"
version = "0.1.0"
description = "AutoGrader.AI monorepo"

[project.optional-dependencies]
dev = ["pytest", "ruff"]
